# ================================================================
# AutoCar III — Steering Self-Calibration Research
# ================================================================
# On AutoCar III (embedded): pip install numpy
# On PC (analysis):          pip install -r requirements.txt

# --- Core (required on both AutoCar III and PC) ---
numpy>=1.17,<1.20; python_version < "3.7"
numpy>=1.20; python_version >= "3.7"

# --- Analysis (PC only) ---
matplotlib>=3.5
scipy>=1.7
pandas>=1.3

# --- Optional ---
# seaborn>=0.12         # Enhanced statistical plots (not required)
# cvxpy>=1.3            # For dense MPC QP (not used — solved via numpy)
